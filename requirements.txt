# Production dependencies
nanobot-ai>=0.1.4
pywebview>=5.0
flask>=3.0
pyyaml>=6.0
pystray>=0.19
Pillow>=10.0
croniter>=1.3

# Development dependencies (for CI/CD and local testing)
# Install with: pip install -e ".[dev]" or pip install -r requirements.txt
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
ruff>=0.3.0
pyright>=1.1.350